for idx = 1:6
    fileName = sprintf('staticInstron_%d.raw',idx);
    [displ,force] = importfile(fileName);
    plot(displ,force)
    hold on
    difForce = diff(force);
    minVal = min(difForce);
    threshold = minVal/5;
    forceOfInterest = 
end